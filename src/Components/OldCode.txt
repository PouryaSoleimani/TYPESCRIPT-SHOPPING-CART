
        const copy = [...BAG]
        copy.push({ ...product, count: 1 })
        setBAG(copy)

        const mainProductInCart = copy.find(Product => Product.id === product.id)
        console.log(mainProductInCart)

        if (mainProductInCart) {
            return copy.map(Product => {
                if (Product.id === product.id) {
                    return { ...product, count: product.count + 1 }
                } else {
                    return product
                }
            })
        }



        // SECOND CODES
                setBAG((prevProducts) => {
            const mainProductInBag = prevProducts.find(Product => Product.id === product.id)
            if (mainProductInBag) {
                prevProducts.map(Product => {
                    if (Product.id === product.id) {
                        return { ...Product, count: Product.count + 1 }
                    } else {
                        return Product
                    }
                })
            } else {
                const mainProductInShop = allproducts.find(Product => Product.id === product.id) as ProductType | undefined;
                return [...prevProducts, { ...mainProductInShop, count: 1 }]
            }

        })

